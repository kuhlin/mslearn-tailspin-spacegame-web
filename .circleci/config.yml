version: 2
jobs:
  build:
    docker:
      - image: 'Ubuntu-16.04'

    steps:
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world.

      - run:
          name: Print the Current Time
          command: date      
      
      - run:
          name: Run npm install
          command: npm